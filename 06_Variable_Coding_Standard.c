#include <stdio.h>

/*Try using Hungarian notation. The notation is an attempt to formalize how a variable’s name can indicate its type or purpose.*/

int main() {
	char *sObjectName = "Circle";		//s indicates string 
	int  iNoOfObjects = 40;				//i indicates integer 
	float fRadius = 10.0; 				//f indicates float
	return 0;
}
